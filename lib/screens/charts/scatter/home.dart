import 'package:flutter/material.dart';

class ScatterHome extends StatelessWidget {
  const ScatterHome({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
    );
  }
}